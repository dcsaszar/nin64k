MEMORY {
    LOADADDR: start = $07FF, size = $0002;
    CODE:     start = $0801, size = $07FF, fill = yes, fillval = $00;
    MAIN:     start = $1000, size = $EFFE;
}

SEGMENTS {
    LOADADDR: load = LOADADDR;
    CODE:     load = CODE;
    PART1:    load = MAIN;
    DATA:     load = MAIN;
}
